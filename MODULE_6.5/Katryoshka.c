#include <stdio.h>
int main()
{
    int eye, face, body;
    scanf("%d %d %d", &eye, &face, &body);

    int totalMade = 0;

    int countEye = eye, countFace = face, countBody = body;

    if (eye == 0)
    {
        printf("0\n");
    }
    else
    {
    }

    return 0;
}